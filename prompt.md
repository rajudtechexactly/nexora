You are a senior-level software architect, Laravel expert, PostgreSQL expert, NativePHP mobile engineer, and scalable social-media application architect.

Your task is to build a social media mobile application similar to Facebook (but NOT copying Facebook branding or UI exactly). The application must contain modern social networking features expected in a real-world platform.

The application name should be unique and original.

Core Requirements

Build a COMPLETE FULL-STACK MOBILE APPLICATION using:

Laravel 13
PHP 8.3
NativePHP for mobile app
PostgreSQL
Laravel Reverb
Laravel Queues
Laravel JWT for authentication
FFmpeg for video processing
WebRTC for video/audio calling
Monolithic Architecture (single application)
REST API based architecture inside monolith
Clean Architecture + Service Layer + Repository Pattern
Dockerized environment
Production-ready codebase
Scalable modular monolith structure

The system must support Android using NativePHP.

Create a facebook clone mobile app, not as it is facebook but must contain the expected features like real fcaebook and chatting, reels, video streaming, video calling, post, freiend request- accept / reject /block , etc all other features. and make this app using Laravel + native-php (we use native-php for mobile app) and postgresql as database . application should a single application follow the monolithic architecture. 

I giving the database creds given below: 
DB_CONNECTION=pgsql 
DB_HOST=ep-dark-dawn-aqxm1o18-pooler.c-8.us-east-1.aws.neon.tech 
DB_PORT=5432 DB_DATABASE=neondb 
DB_USERNAME=neondb_owner 
DB_PASSWORD=npg_0XOyGoIB2pft 

Also these are the mail credentials (for authentication/ reset password / any type of functinality for sending mail) 
MAIL_MAILER=log 
MAIL_SCHEME=null 
MAIL_HOST=smtp.gmail.com 
MAIL_PORT=465 
MAIL_USERNAME=noreply@ggapparels.in 
MAIL_PASSWORD="ykin eqyi ccmn ssqo" 
MAIL_FROM_ADDRESS="hello@example.com" 
MAIL_FROM_NAME="${APP_NAME}"